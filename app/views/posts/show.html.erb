<% provide(:subtitle_lv1, @post.topic.content) %>
<% provide(:title, @post.subject) %>
<% provide(:subtitle_lv2, @post.subject) %>
<section class="games-single-page">
  <div class="container">
    <%= render "layouts/pagetop" %>
    <div class="row">
      <div class="col-xl-9 col-lg-8 col-md-7 game-single-content">
        <h2 class="gs-title"><%= @post.subject %></h2>
        <div class="gs-meta">
          <%= t "posts.show.time", name: time_ago_in_words(@post.created_at) %>
          <%= t ".in" %> <%= link_to @post.topic.content, "#" %>
        </div>
        <p class="text-white"><%= @post.content.html_safe %></p>
        <div class="geme-social-share pt-5 d-flex">
          <p><%= t ".share" %></p>
          <%= link_to "#" do %>
            <i class="fa fa-pinterest"></i>
          <% end %>
          <%= link_to "#" do %>
            <i class="fa fa-facebook"></i>
          <% end %>
          <%= link_to "#" do %>
            <i class="fa fa-dribbble"></i>
          <% end %>
            <%= link_to "#" do %>
          <i class="fa fa-twitter"></i>
          <% end %>
          <%= link_to "#" do %>
            <i class="fa fa-behance"></i>
          <% end %>
        </div>
        <section class="game-author-section">
          <div class="container">
            <div class="game-author-pic set-bg" data-setbg="/assets/author.jpg"></div>
            <div class="game-author-info">
              <h4><%= t ".write_by" %> <%= @post.user_name %></h4>
            </div>
          </div>
        </section>
        <div class="container">
          <div class="row">
            <div class="col-md-10">
              <section class="comment-list">
                <div class="reply-post w-100">
                  <%= render "layouts/error" %>
                  <h3 class="page-header text-white"><%= t ".write_a_comments" %></h3>
                  <%= bootstrap_form_for [@post, @comment] do |f| %>
                    <%= f.text_field :content, hide_label: true %>
                    <%= f.submit t(".comment") %>
                  <% end %>
                </div>
                <h2 class="page-header text-white"><%= t ".comment" %></h2>
                <% @comments.each do |comment| %>
                  <article class="row one-comment">
                    <div class="col-md-2 col-sm-2 hidden-xs">
                      <figure class="thumbnail">
                        <%= image_tag "review/user-avatar-placeholder.png", class: "img-responsive" %>
                      </figure>
                    </div>
                    <div class="col-md-10 col-sm-10">
                      <div class="panel panel-default arrow left">
                        <div class="panel-body">
                          <header class="text-left">
                            <div class="comment-user text-white">
                              <i class="fa fa-user text-white"></i>
                              <%= comment.user.name %>
                            </div>
                            <time class="comment-date text-white">
                              <i class="fa fa-clock-o text-white"></i>
                              <%= t "posts.show.time", name: time_ago_in_words(comment.created_at) %>
                            </time>
                          </header>
                          <div class="comment-post">
                            <p class="text-white">
                              <%= comment.content %>
                            </p>
                          </div>
                        </div>
                      </div>
                    </div>
                    <%= render partial: "comments/comment",
                      collection: comment.sub_comments, as: :comment %>
                    <div class="subcomment"></div>
                    <!-- Form reply comment -->
                    <div class="d-flex flex-row-reverse w-100">
                      <div class="col-md-10 col-sm-10">
                        <article class="row one-comment">
                          <%= bootstrap_form_for [@post, @comment], remote: true do |f| %>
                            <%= f.text_field :content, hide_label: true, class: "form-control reset-text" %>
                            <p class="btn btn-default btn-sm text-white text-right">
                              <i class="fa fa-reply text-white"></i>
                              <%= f.submit t(".reply"), class: "btn btn-secondary button-custom" %>
                            </p>
                          <% end %>
                        </article>
                      </div>
                    </div>
                    <!-- End form reply comment -->
                  </article>
                <% end %>
              </section>
              <%= will_paginate @comments, page_links: false %>
            </div>
          </div>
        </div>
      </div>
      <div class="col-xl-3 col-lg-4 col-md-5 sidebar game-page-sideber">
        <div id="stickySidebar">
          <div class="widget-item">
            <div class="rating-widget">
              <h4 class="widget-title"><%= t ".suggest" %></h4>
              <!-- This area for suggest item -->
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
